// A prompts URL without a number is not a step of its own. The layout replaces
// it with the tool's first prompt, so there is nothing to render here.
export default function LensPromptsIndexPage() {
  return null
}
