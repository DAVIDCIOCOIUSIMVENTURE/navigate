// A questions URL without a number is not a step of its own. The layout
// replaces it with the tool's first question, so there is nothing to render here.
export default function GuidedQuestionsIndexPage() {
  return null
}
