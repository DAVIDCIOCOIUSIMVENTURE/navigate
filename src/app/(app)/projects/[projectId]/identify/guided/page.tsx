// The bare tool URL is not a step of its own. The layout replaces it with
// wherever the tool was left, or its first question, so there is nothing to
// render here.
export default function GuidedIndexPage() {
  return null
}
