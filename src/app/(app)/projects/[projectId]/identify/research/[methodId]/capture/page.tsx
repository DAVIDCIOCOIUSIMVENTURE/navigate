// A capture URL without a number is not a step of its own. The research
// layout replaces it with the method's first prompt, so there is nothing to
// render here.
export default function ResearchCaptureIndexPage() {
  return null
}
