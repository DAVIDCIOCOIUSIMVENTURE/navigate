// A bare method URL is not a step of its own. The research layout replaces it
// with the method's tool step, so there is nothing to render here.
export default function ResearchMethodPage() {
  return null
}
