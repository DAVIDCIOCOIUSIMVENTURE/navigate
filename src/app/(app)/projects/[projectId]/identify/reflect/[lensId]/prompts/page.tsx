// A prompts URL without a number is not a step of its own. The reflect layout
// replaces it with the lens's first prompt, so there is nothing to render here.
export default function ReflectPromptsIndexPage() {
  return null
}
