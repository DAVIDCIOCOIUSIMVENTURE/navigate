// A bare lens URL is not a step of its own. The reflect layout replaces it
// with the lens's first prompt, so there is nothing to render here.
export default function ReflectLensPage() {
  return null
}
